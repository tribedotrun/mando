//! Evidence download (legacy).
//!
//! Evidence is now managed by the CLI (mando todo evidence) which saves
//! files locally and registers metadata via the daemon API. This module
//! is kept as an empty namespace.
