//! Session lifecycle state-machine helpers — provided by `sessions-db`.
//!
//! `infer_command` and `SessionLifecycleCommand` are available via
//! `sessions_db::lifecycle` for callers that need them directly.
