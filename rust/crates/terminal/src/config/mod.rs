//! Terminal config tier.
//!
//! The terminal subsystem currently reads its runtime config through the
//! shared settings handle, so this tier is intentionally light but present to
//! preserve the standard crate envelope.
